import { StyleSheet } from "react-native";

export const authScreenStyles = StyleSheet.create({
  p: {
    textAlign: "center",
    marginTop: "10%",
    marginBottom: 20,
  },
  btn: {
    marginTop: 10,
  },
});
